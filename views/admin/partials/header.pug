header.header
  .container-fluid
    .row.align-items-center
      .col-3
        .inner-logo
          a(href=`/${adminPrefix}/dashboard`) ADMIN

      .col-9
        div(class="text-right")
          a(
            href=`/${adminPrefix}/my-account`
            class="btn btn-primary btn-sm mr-2"
          ) #{user.fullName}
          a(
            href=`/${adminPrefix}/auth/logout`
            class="btn btn-danger btn-sm"
          ) Logout