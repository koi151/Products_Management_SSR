extends ../../layouts/default.pug
include ../../mixins/box-head.pug

block main 
  .container.my-3 

    include ../../partials/list-user-buttons.pug

    .row 
      .col-12(class='mt-2') 
        +box-head('User List')

    //-  first user.id is blobal variable, the next one is for loop
    .row(data-users-not-friend=user.id)
      each user in users
        .col-6(user-id=user.id) 
          div(class='box-user')
            div(class='inner-avatar')
              img(
                src=(user.avatar ? user.avatar : "/images/img_avatar.png")
                alt=user.fullName
              )
            div(class='inner-info')
              div(class='inner-name') #{user.fullName}
              div(class='inner-buttons')
                button(
                  class='btn btn-sm btn-primary mr-1'
                  btn-add-friend=user.id
                ) Add friend
                button(
                  class='btn btn-sm btn-secondary mr-1'
                  btn-cancel-friend=user.id
                ) Cancel

  block script
    script(type='module' src='/js/users.js')
    


